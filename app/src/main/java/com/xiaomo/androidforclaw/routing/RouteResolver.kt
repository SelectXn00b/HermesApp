package com.xiaomo.androidforclaw.routing

/**
 * OpenClaw module: routing
 * Source: OpenClaw/src/routing/
 *
 * Marker object kept for backward compatibility.
 * All routing logic now lives in:
 * - AccountId.kt   -- account ID normalization
 * - SessionKey.kt  -- session key construction and parsing
 * - ResolveRoute.kt -- route resolution, bindings, account lookup
 */
object RouteResolver
