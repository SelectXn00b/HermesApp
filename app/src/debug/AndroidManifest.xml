<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly">

    <!-- Debug版本 - 开发和测试使用 -->
    
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- 基础权限 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    
    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- 服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" tools:node="remove" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <queries>
        <!-- Add package queries here if needed -->
    </queries>

    <application
        android:name="com.xiaomo.androidforclaw.core.MyApplication"
        android:banner="@drawable/banner"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneClaw"
        android:debuggable="true"
        tools:replace="android:debuggable">



        <!-- 主页 - 状态展示 (旧版，已废弃) -->
        <activity
            android:name="com.xiaomo.androidforclaw.ui.activity.MainActivity"
            android:exported="false"
            android:label="PhoneClaw Debug (Legacy)"
            android:theme="@style/Theme.PhoneClaw"
            tools:replace="android:exported" />

        <!-- 配置页面 -->
        <activity
            android:name="com.xiaomo.androidforclaw.ui.activity.ConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- Skills 管理页面 -->
        <activity
            android:name="com.xiaomo.androidforclaw.ui.activity.SkillsActivity"
            android:exported="false"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- Agent 测试页面 -->
        <activity
            android:name="com.xiaomo.androidforclaw.ui.activity.AgentTestActivity"
            android:exported="false"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- 权限管理页面 -->
        <activity
            android:name="com.xiaomo.androidforclaw.ui.activity.PermissionsActivity"
            android:exported="true"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- QuickJS 测试页面 -->
        <activity
            android:name="com.xiaomo.androidforclaw.ui.activity.QuickJSTestActivity"
            android:exported="true"
            android:label="QuickJS Test"
            android:theme="@style/Theme.PhoneClaw">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Store: 移除前台服务 -->
        <service android:name="com.xiaomo.androidforclaw.core.ForegroundService" tools:node="remove" />

        <!-- ADB输入法服务 -->
        <service
            android:name="com.xiaomo.androidforclaw.service.AdbIME"
            android:exported="false"
            android:permission="android.permission.BIND_INPUT_METHOD"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/methods" />
        </service>


            

        <!-- AI测试Activity -->
        <activity android:name="com.xiaomo.androidforclaw.ui.activity.AITestActivity"
            android:exported="false"
            android:theme="@style/Theme.PhoneClaw" />
        
        <!-- Web服务 -->
        <service
            android:name="com.xiaomo.androidforclaw.service.WebService"
            android:enabled="true"
            android:exported="false"
            tools:replace="android:exported" />

        <!-- Agent 消息接收器 -->
        <receiver
            android:name="com.xiaomo.androidforclaw.core.AgentMessageReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.xiaomo.androidforclaw.ACTION_EXECUTE_AGENT" />
            </intent-filter>
        </receiver>

        <!-- 文件提供者 -->
        <provider
            android:name="com.xiaomo.androidforclaw.ClawFileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest> 